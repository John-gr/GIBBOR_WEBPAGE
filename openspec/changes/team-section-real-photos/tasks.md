## Tasks

- [x] Add the real team photos to `public/team/` with filenames that correspond to each member.
- [x] Update `src/components/landing/TeamSection.jsx` to use `/team/<filename>` for each member's `image` property.
- [x] Correct the team branding text so the team is named `Gibborim` and the product/project remains `Gibbor`.
- [x] Verify the `TeamPage` renders correctly and that each image loads with the proper `alt` text.
- [ ] Preview the site locally and confirm the new static images work as expected on Vercel.
