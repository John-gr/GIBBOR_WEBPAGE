## Tasks

- [x] Verify that `public/logo_placeholder.png` exists and is the desired logo asset.
- [x] Update `index.html` to point the favicon `<link rel="icon">` to `/logo_placeholder.png`.
- [ ] Preview the site locally and confirm the browser tab shows the new logo.
- [ ] If desired, verify the favicon in a Vercel preview deployment after pushing the change.
